#ifndef __SCHED_DEFAULT_H__
#define  __SCHED_DEFAULT_H__

#include <eza/scheduler.h>
#include <eza/arch/types.h>

typedef struct __eze_sched_data {
} eza_sched_data_t;

typedef struct __cpu_runqueue {
} cpu_runqueue_t;

typedef struct __eza_sched_cpuinfo {
  
} eza_sched_cpuinfo_t;

#endif
